# 名片移动浏览执行计划

## 当前提交安排

用户已授权将本任务待提交改动合为一个 `feat(web): redesign mobile namecard browsing` 工作提交，范围见 `research/commit-plan.md`。下文未提交的表述保留为各轮验证时的历史状态。旋转组合问题仍未解决，不据此归档或标记任务完成。

## 当前修订：分钟级时间

- [x] 列表改为北京时间 `MM-DD HH:mm`，保留完整说明、ISO 数据及异常提示。
- [x] 982 项单测、54 项相关 Web、10 项 App 浏览器测试及生产构建通过；iOS 模拟器 LAN Release 已更新。
- [x] 原生竖屏 `run-15` 通过，已核对分钟时间、4+2 反应、键盘及 24 张选择截图，并同步交付记录；未操作真机或提交 Git。
- [ ] 排查键盘跳页后预览横竖屏切换、关闭导致日期遮挡的组合；保留失败用例，不以竖屏通过替代。

## 当前修订：紧凑反应区

- [x] 缩小移动列表反应与计数，包括加号在内每行最多 4 项，保留 44px 触点和完整数字；选择器及桌面保持原样。
- [x] 982 项 Web 单测、54 项相关 Web E2E、10 项 App 浏览器测试及生产构建通过；包含 402px 四项同排、320/390px 换行和 48 张六位数计数压力检查。
- [x] 更新 iOS 模拟器 LAN Release；原生竖屏分页、键盘、表情、预览及数量选择通过并检查截图。旋转组合仍待处理。
- [x] 更新交付证据和提交计划；未提交 Git、未操作真机。

## 本轮反馈修复

- [x] 修复 App 分页与悬浮上传/返回顶部的冲突，五视口验证进入/离开分页及真实触点，单测覆盖其他路由的限定条件。
- [x] 日期改为简短数字日期，保留完整语义、时区跨日及非法值处理；320px 实测日期在条目内完整显示。
- [x] 引入全部 46 种本地 Twemoji 图形，保存固定版本、许可、署名、SHA256 和资产完整性测试。
- [x] check:web 通过 979 项单测与生产构建；51 项相关 Web 和 10 项 App 浏览器测试通过，148 项聚焦单测通过。
- [x] iOS 与 Android 最新 LAN Release 均安装成功，58 个相关源码/资产文件的聚合 SHA256 一致。Android 已启动并验签/核对手机内 APK；iPhone 锁屏，安装成功但未自动启动。
- [x] R6 同源 LAN Release 已安装至 iPhone 17 Pro / iOS 26.5 模拟器；原生 XCTest 的分页避让、46 项表情、软件键盘跳页、切面、切卡复位、横屏和底栏恢复通过。R7 后需更新包与复测，不将旧结果记为新版本验收。

最新证据见 [验证记录](./research/validation.md) 和 [设备交付](./research/device-delivery.md)。下文保留此前各轮的历史状态，不能用于判断当前包版本。

## 上次修订与交付

自然排列、整体 Card 和两行移动分页均已通过验证，并纳入两端局域网 Release 构建。Android 最新版已安装；iOS 构建与签名验证成功，但安装和复用包重试均因设备不可达而失败，需要连接并解锁 iPhone。见 [验证记录](./research/validation.md) 与 [设备交付](./research/device-delivery.md)。

- [x] 删除固定位移、底部占位和手机等高约束，加入页私有 ResizeObserver 布局，保持原始 DOM/预览顺序。
- [x] 双面、时间、emoji 和认领信息共享一个可见 Card，使用现有背景、轻边框、8px 圆角和紧凑内边距。
- [x] 修复 Firefox 高内容网格上限问题：改用顶底边界划分的有限显式轨道，加入 48 张、11 种六位数计数的单测与三浏览器回归。
- [x] 验证等高、不同 emoji 数量、异步变化、宽度切换、同列 12px 间距及末项/分页分离。
- [x] 分页主行放置前后页、页码和跳转，次行放置总量与每页数量；支持 Enter 提交、Escape 取消、原页禁用和新页起始内容可见，保持 44px 控件与稳定可访问名称。
- [x] 最新检查通过：903 项 Web 单测、51 项相关 Web E2E、5 项 App E2E、ESLint/typecheck、生产构建、规则检查、LSP 与 diff 检查。
- [x] 使用命令级 LAN origin/签名参数串行构建两端，核对均包含当前六个 UI 源文件的相同 SHA256；未改 env 文件、原生插件或安装脚本。
- [x] Android 最新版安装成功，核对手机内 APK 哈希、签名、版本及运行进程；设备息屏，未宣称真机画面验收。
- [x] iOS Release 构建与签名检查通过，描述文件包含目标 iPhone；记录签名到期时间。
- [ ] iPhone 恢复连接后复用当前包安装并启动，再核对真机局域网读取。
- [ ] 两端解锁后确认名片与分页的实际画面；不提交 Git。

## 上一版完成记录

以下是固定错位版本的执行历史；新布局验收以前述待办为准。上一版实现与验收已完成，尚未提交 Git。完整证据、日志、截图和全量 E2E 的限制见 [最终验证记录](./research/validation.md)。任务保留 `in_progress`，不提前提交、归档或记录完成提交。

当时确认的要求：手机双列，每张名片正反上下同显，首行顶部对齐，从第二行起右列轻微下移 24px。桌面不位移，保留正反并排；预览、分页及业务规则按已批准的 PRD 和设计执行。

## 执行记录

### 1. 准备与基线

- [x] 获得范围、双列双面及错位设计的实施批准，后续落实顶部对齐要求。
- [x] 读取准备技能、根与 Web 规则、目标规范，对照 `DESIGN.md` 和 `app.css`。
- [x] 核对 `release/v1.1` 工作区并执行 `task.py start`；未切换主工作区分支。
- [x] 运行环境预检。5173/6379 已占用，复用 API 3000，以正式 Web 入口在 5181 启动预览；不重建数据服务。
- [x] 运行原有名片单测及浏览器基线并保存真实图片截图。

### 2. 列表与条目

- [x] 手机采用行优先双列网格，320px 不退回单列；每张名片内部正面在上、背面在下，桌面正反并排。
- [x] 收紧标题和条目留白；使用约 8px 列间距、4px 两面间距和 12px 条目间距，保留 DOM、键盘与预览顺序。
- [x] 首行顶部对齐，从第二行起右列下移 24px，底部预留对应空间，桌面清除位移；添加相关几何断言。
- [x] 固定 3:2 图片舞台使用 contain，支持加载、错误和一次性原图回退；点击任一面打开对应原图。
- [x] 移动条目去掉外框和独立灰底反应区，保留桌面样式。
- [x] 保留完整反应集合、计数、点击限制、44px 目标、认领状态和上传入口所有权。
- [x] 添加页面和图片组件回归，覆盖两面、分页、加载、重试、回退及匿名/登录状态。

### 3. 连续预览状态

- [x] 页私有 `use-namecard-preview-navigation.ts` 通过现有 API 门面读取相邻页，预览状态不改写列表 URL。
- [x] 覆盖同页/跨页、首尾、同步请求锁、失败重试、空响应和关闭/重开/上下文变化后的响应失效。
- [x] `use-namecard-preview-return.ts` 保存原触发控件、祖先与窗口位置；覆盖延迟恢复和新会话之间的竞争。
- [x] 导航与返回 hook 分别通过 17 项和 8 项单元测试，并经 Web/App 浏览器确认。

### 4. 预览交互与布局

- [x] `NamecardPreview` 接受可选导航状态，切卡保持单 Dialog 挂载，复位面、缩放和拖动记录。
- [x] 小屏工具分组，避让四侧安全区；保留方向键、Escape、静止空白关闭和焦点恢复。
- [x] 使用 `sr-only` ID 保留无障碍标题兼容，可见标题不展示内部编号。
- [x] 修正 Firefox 密集反应弹层的中间透明度/缩放残留，仅对该选择器取消动画；断言最终 opacity 和实际触控尺寸。

### 5. 浏览器与质量检查

- [x] 保留预览、分页、上传回归；新增 Web/App 移动浏览与可检查边角的 PNG fixture。
- [x] 同步旧大图测试的正面/背面控件名称，保留其行为断言。
- [x] Web 相关 42 项及 App 五视口 5 项浏览器测试通过，包含 axe、无溢出、安全区、控件几何和返回位置。
- [x] 验证 390x844 首屏两张完整双面及下一行起始；320px 下额外检查大量六位数反应和认领徽章，不裁切内容或缩小点击目标。
- [x] 保存真实名片的桌面、移动亮色/暗色及 Firefox 弹层截图。
- [x] `pnpm run check:web` 通过：ESLint、typecheck、165 文件 891 项单测、client/SSR 构建。
- [x] 执行格式化；默认脚本因缺失可选 docs 目录失败，追加 `--no-error-on-unmatched-pattern` 后通过，未保留无关格式化改动。
- [x] 执行全量 `pnpm run test:web` 并记录未全绿结果；未越界修复首页、后台、交换区。
- [x] Trellis check 审阅全部源代码、单测和 E2E，修复本任务问题；确认 API、契约、路由、壳层和全局 token 未改变。

## 验证命令与环境

```sh
pnpm --filter @imsweb/web run format --no-error-on-unmatched-pattern
pnpm run check:web
pnpm run test:web
pnpm --filter @imsweb/web run test:e2e:app app-namecard-browsing.spec.ts
pnpm run check:rules
```

Web 聚焦命令及准确选择范围见最终验证记录。此前 Web E2E 使用 `E2E_BASE_URL=http://127.0.0.1:5181`；用户现要求关闭 5181，后续观察与复测使用 5173。Web/App 开发服务器串行验证，避免热更新干扰。该版验收未涉及原生设备；当前设备请求另见设备交付记录。

已停止并删除基线复测使用的临时服务与工作树。5181 已关闭，当前观察地址为 `http://127.0.0.1:5173/community/cards`；页面和 API 均返回 HTTP 200。日志及截图保存在忽略目录 `data/qa/namecard-mobile-results/`。

## 收尾与回滚

- [x] 按 AC1 至 AC10 记录证据及测试限制。
- [x] 读取 `trellis-update-spec`，记录预览会话、焦点恢复和弹层最终样式检查约定，并更新规范索引。
- [x] 用户确认本任务源代码、测试、规范和任务文档合为一个工作提交；不改写历史、不 amend、不 push。
- [ ] 提交完成后再执行任务归档与 journal 收尾。

回滚只针对本任务确认过的文件。不得整体 reset、覆盖用户修改或更改现有数据库、缓存与对象存储配置。
