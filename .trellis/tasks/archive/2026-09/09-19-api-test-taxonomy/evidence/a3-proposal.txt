### tests/server/migration-catalog.test.ts  (2 cases)
  subject: "migration catalog"  matches 2/2  (byte-exact for every case)
  categories: none (subject has 2 cases, 0 usable category)
    - "copies SQL files only before an existing boundary"
    - "fails before creating a fixture for a missing boundary"
### tests/server/namecard-media-keys.test.ts  (11 cases)
  subject: "claimed"  matches 3/11  (fallback cases keep their name)
  categories: "ensureNamecardThumbnails", "namecard"
    - "card media keys stay readable by the namecard readers"
    - "legacy media keeps the canonical namecards layout"
    - "media keeps the source extension and never reuses the source"
    - "media round-trips through the public media chain"
    - [ensureNamecardThumbnails] "generates missing thumbnails from originals"
    - [ensureNamecardThumbnails] "rejects when the original object is missing"
    - [ensureNamecardThumbnails] "skips sides whose thumbnails already exist"
    - "legacy thumbnail paths map back to the canonical thumbnail key"
    - [namecard] "media key pairs cover the original and its stored thumbnail"
    - [namecard] "thumbnail keys share the original stem under a thumbnail role"
    - [namecard] "thumbnail public URLs keep the original filename identity"
### tests/server/namecard-metadata-repository.test.ts  (2 cases)
  subject: "PostgreSQL guest namecards"  matches 2/2  (byte-exact for every case)
  categories: none (subject has 2 cases, 0 usable category)
    - "may carry optional profile text"
    - "persist ordered cross-series idol metadata atomically"
### tests/server/namecard-ownership-migration.test.ts  (1 cases)
  subject: "namecard ownership migration preserves historical rows"  matches 1/1  (byte-exact for every case)
  categories: none (subject has 1 cases, 0 usable category)
    - "as legacy"
### tests/server/namecard-reaction-reconciliation-migration.test.ts  (1 cases)
  subject: "namecard reaction reconciliation resyncs namecard_reactions from"  matches 1/1  (byte-exact for every case)
  categories: none (subject has 1 cases, 0 usable category)
    - "card_emojis drift"
### tests/server/namecard-unification-migration.test.ts  (1 cases)
  subject: "namecard unification migration folds legacy cards"  matches 1/1  (byte-exact for every case)
  categories: none (subject has 1 cases, 0 usable category)
    - "into one table"
### tests/server/news-pagination.test.ts  (5 cases)
  subject: "news"  matches 4/5  (fallback cases keep their name)
  categories: none (subject has 5 cases, 0 usable category)
    - "admin news parses exact success and mutation business-error envelopes"
    - "cursor pagination holds an id snapshot while rows are inserted"
    - "cursor pagination validates limits, cursors, and empty snapshots"
    - "keeps its legacy array response when pagination is not requested"
    - "list responses preserve a title with legacy leading or trailing whitespace verbatim"
### tests/server/node-email-delivery-runner.test.ts  (14 cases)
  subject: "email"  matches 13/14  (fallback cases keep their name)
  categories: none (subject has 14 cases, 1 usable category)
    - [delivery runner] "cancels a claim when lease renewal is lost false result"
    - [delivery runner] "cancels a claim when lease renewal is lost rejection"
    - [delivery runner] "cancels a claim when lease renewal is lost timeout"
    - [delivery runner] "claims with a fresh post-maintenance timestamp"
    - [delivery runner] "contains claim failures and continues polling"
    - [delivery runner] "drains claims committed while shutdown begins"
    - [delivery runner] "leaves delayed claims for lease recovery after shutdown times out"
    - [delivery runner] "polls immediately and enforces bounded concurrency"
    - [delivery runner] "readiness expires and refreshes after a successful poll"
    - [delivery runner] "recovers readiness after a failed PostgreSQL poll"
    - [delivery runner] "serializes lease renewals"
    - [delivery runner] "waits for graceful work and bounds stalled shutdown"
    - "worker health follows runner readiness, stopping, and liveness"
    - "shutdownEmailWorker bounds stalled service and active health-server close"
### tests/server/object-cleanup-lifecycle.test.ts  (2 cases)
  subject: "Node service shutdown waits for object"  matches 1/2  (fallback cases keep their name)
  categories: none (subject has 2 cases, 0 usable category)
    - "cleanup before closing its dependencies"
    - "object cleanup runner stops scheduling and closes only after the active cycle is idle"
### tests/server/object-deletion-worker-fencing.test.ts  (1 cases)
  subject: "object deletion lease fences stale worker"  matches 1/1  (byte-exact for every case)
  categories: none (subject has 1 cases, 0 usable category)
    - "failure after takeover"
### tests/server/object-deletion-worker.test.ts  (1 cases)
  subject: "PostgreSQL object deletion worker retries and"  matches 1/1  (byte-exact for every case)
  categories: none (subject has 1 cases, 0 usable category)
    - "atomically leases prefix jobs"
### tests/server/object-protection-compensation.test.ts  (1 cases)
  subject: "object protection and its compensation are"  matches 1/1  (byte-exact for every case)
  categories: none (subject has 1 cases, 0 usable category)
    - "fenced to one object version"
### tests/server/object-read-response.test.ts  (3 cases)
  subject: "S3-capable media responses redirect GET and"  matches 1/3  (fallback cases keep their name)
  categories: none (subject has 3 cases, 0 usable category)
    - "HEAD without loading object bytes"
    - "proxy mode bypasses signed URLs and preserves stored byte semantics"
    - "public redirects receive a bounded cache policy when the handler has none"
### tests/server/optional-platform-auth.test.ts  (3 cases)
  subject: "optional Platform auth"  matches 3/3  (byte-exact for every case)
  categories: none (subject has 3 cases, 0 usable category)
    - "exposes only a fully validated Platform viewer"
    - "rejects invalid credentials instead of downgrading"
    - "stays anonymous when Platform credentials are absent"
### tests/server/platform-account-admin-repository.test.ts  (7 cases)
  subject: "admin list"  matches 2/7  (fallback cases keep their name)
  categories: none (subject has 7 cases, 0 usable category)
    - "a stale status write reports a conflict and leaves sessions alone"
    - "activating a restricted account is unsupported and unknown ids are not-found"
    - "counts only live refresh sessions and reports them without leaking hashes"
    - "searches by id, email and display name and hides deleted accounts"
    - "force logout revokes every live session, bumps the version and is idempotent"
    - "suspending bumps token_version, sweeps live sessions and is not affected by a lost race"
    - "the last-credential guard still refuses unlinking an OAuth-only account"
### tests/server/platform-account-management-repository.test.ts  (26 cases)
  subject: "a"  matches 7/26  (fallback cases keep their name)
  categories: "an", "binding", "two"
    - "concurrent exchange redeems the code for exactly one caller"
    - "link state round-trips its account and intent"
    - "second subject for the same provider on one account is a provider conflict"
    - "stale migration expectation reports state-conflict"
    - "stale status revision reports conflict with the current projection"
    - "subject owned by another account is refused without a write"
    - "wrong binding code leaves the credential unwritten"
    - "activating a restricted account is unsupported"
    - "admin listing searches by id, email and display name and excludes deleted accounts"
    - [an] "app state carries its challenge and survives the read-only return-channel lookup"
    - [an] "exchange code is consumed exactly once"
    - [an] "expired exchange code is not returned"
    - [binding] "a second credential for one account is already-bound and keeps the code"
    - [binding] "a taken address reports email-conflict and rolls the code back"
    - [binding] "an email credential consumes the code and survives a password hash"
    - "forcing a logout revokes every live session and is idempotent"
    - "linking an identity writes the row and its audit event"
    - "linking to a missing or inactive account is not found"
    - "migrating an email keeps the password hash, algorithm and salt"
    - "migrating without a bound credential reports not-bound"
    - "re-linking the same subject to the same account is idempotent"
    - "reactivating a suspended account restores it without bumping twice"
    - "suspending an account bumps the token version and revokes live sessions atomically"
    - [two] "concurrent binds for one account produce exactly one credential"
    - [two] "concurrent links of one subject resolve to exactly one owner"
    - [two] "unlinks cannot strip every login method"
### tests/server/platform-account-security.contract.test.ts  (38 cases)
  subject: "password change"  matches 14/38  (fallback cases keep their name)
  categories: "rejects", "a", "the", "an"
    - [a] "disabled provider does not count as a surviving login method"
    - [a] "password makes a sole OAuth link removable"
    - [a] "password-less account cannot unlink its only provider"
    - [a] "restricted account cannot write, but can still read its devices"
    - [a] "session owned by another account cannot be revoked"
    - [a] "successful password change keeps this session and drops the others"
    - [a] "wrong current password is refused without disturbing the account"
    - "account security endpoints carry their own rate limit buckets"
    - [an] "account without an email credential cannot change a password"
    - [an] "empty provider string is reported as no value, not as a blank name"
    - [an] "unknown provider and another account's link are both just missing"
    - "anonymous and restricted callers cannot unlink"
    - "cookie callers must present a matching CSRF token"
    - "cookie callers must present a matching CSRF token to unlink"
    - "packaged clients receive the rotated tokens in the body"
    - "refuses to reuse the current password"
    - [rejects] "array body"
    - [rejects] "blank current password"
    - [rejects] "malformed JSON"
    - [rejects] "missing current password"
    - [rejects] "missing new password"
    - [rejects] "new password over 128 characters"
    - [rejects] "new password over 72 UTF-8 bytes"
    - [rejects] "new password under 8 characters"
    - [rejects] "non-string new password"
    - [rejects] "null body"
    - [rejects] "null new password"
    - [rejects] "unknown field"
    - "requires a JSON content type"
    - "platform account security raw JSON conforms across password, sessions, and OAuth unlink"
    - "platform account security rejects anonymous callers"
    - "revoking one session only reaches this account"
    - "signing out everywhere else keeps the calling session"
    - [the] "OAuth link endpoints carry their own rate limit buckets"
    - [the] "OAuth link list never exposes the third-party subject"
    - [the] "link list reports whether a password exists at all"
    - [the] "session list never exposes a session secret"
    - "without a password, one of two enabled links can still go"
### tests/server/platform-email-auth.contract.test.ts  (20 cases)
  subject: "migrated PBKDF2"  matches 2/20  (fallback cases keep their name)
  categories: none (subject has 20 cases, 0 usable category)
    - "Platform email auth routes use independent IP rate-limit buckets"
    - "bcrypt rejects passwords beyond 72 UTF-8 bytes instead of truncating"
    - "bearer callers get tokens from registration and login, cookie callers do not"
    - "cache failure falls through to SQL cooldown with exact Retry-After"
    - "configured resend intervals and unknown password reset responses come from PostgreSQL"
    - "durable HTTP enqueue is completed by a fresh worker runner"
    - "email auth strictly validates JSON shapes and credential fields"
    - "login account limiting shares a normalized digest across rotating IPs before lookup"
    - "login returns one generic credential error and rejects blocked account states"
    - "long migrated PBKDF2 passwords authenticate without unsafe bcrypt upgrade"
    - "accepts only the declared Fudaba parameter contract"
    - "credential logs in once and upgrades with bcrypt CAS"
    - "password reset cooldown is enumeration-safe with missing or failing cache"
    - "password reset responses preserve exact JSON and reject invalid API email grammar"
    - "real PostgreSQL failed resend preserves old code across repository instances"
    - "real PostgreSQL keeps registration atomic under normalized email races"
    - "registration verification is hashed, cached, atomically consumed, and single use"
    - "session fencing returns account unavailable without writing cookies"
    - "verification enqueue failure returns purpose-specific unavailable response"
    - "verification requests enqueue without SMTP and staged codes remain unusable"
### tests/server/platform-email-binding.contract.test.ts  (24 cases)
  subject: "app OAuth link start"  matches 5/24  (fallback cases keep their name)
  categories: "a", "changing"
    - "OAuth link start demands a session"
    - "OAuth link start writes an intent=link state and redirects to the provider"
    - [a] "provider denial on a link+app state keeps flow=link"
    - [a] "provider that cannot build an authorization URL writes no state"
    - [a] "refused app link callback returns an error with flow=link"
    - [a] "web link callback still returns to account security with a reason"
    - "an app link callback returns a one-time code with flow=link"
    - "an unavailable provider returns to account security with a reason"
    - "demands a bearer session"
    - "rejects an unknown body key"
    - "rejects an unusable challenge"
    - "reports an unavailable provider"
    - "returns the provider URL and records an app state"
    - "binding a second email for an account already bound is refused"
    - "binding requires a valid code and leaves no credential on failure"
    - [changing] "an email needs the current password and keeps the credential on refusal"
    - [changing] "an email on a provider-only account reports not-bound"
    - [changing] "an email preserves the password hash and every live session"
    - [changing] "to an address owned by another account is refused without a partial write"
    - "email binding and OAuth link start use their own account-dimension buckets"
    - "email binding rejects anonymous callers on every route"
    - "platform email binding raw JSON conforms across read, code, and bind"
    - "sending a code for an address owned by another account is refused up front"
    - "the verification code is hashed with the binding domain, not the registration one"
### tests/server/platform-email-cache.test.ts  (5 cases)
  subject: "platform email cooldown"  matches 3/5  (fallback cases keep their name)
  categories: none (subject has 5 cases, 0 usable category)
    - "email cooldown cache operations abort when the backend never settles"
    - "password reset cooldown uses the same strict snapshot without exposing email"
    - "can be cleared after delivery or registration"
    - "rejects malformed records and fails open when unavailable"
    - "uses an anonymous key, strict snapshot, and deadline TTL"
### tests/server/platform-email-delivery-repository.test.ts  (11 cases)
  subject: "password reset"  matches 4/11  (fallback cases keep their name)
  categories: none (subject has 11 cases, 0 usable category)
    - "a 30-second policy supersedes queued, retrying, and running deliveries"
    - "anonymous cooldown survives account creation and uses the next policy after expiry"
    - "competing claims, lease expiry, renewal, and stale owners are fenced"
    - "completion-first and supersession-first ordering preserve the intended candidate"
    - "failed resend preserves the active code and missing candidates never retry"
    - "activation uses acceptance time and remains unusable while queued"
    - "request cooldown serializes simultaneous first requests"
    - "request cooldowns are durable, anonymous, and preserve legacy authority"
    - "supersession and terminal failure preserve the active code"
    - "registration enqueue is atomic, encrypted, and unusable until delivery completes"
    - "retry, attempt, deadline, expiry, and retention limits are enforced"
### tests/server/platform-email-delivery-service.test.ts  (9 cases)
  subject: "worker SMTP delivery"  matches 9/9  (byte-exact for every case)
  categories: none (subject has 9 cases, 0 usable category)
    - "classifies only bounded failure evidence"
    - "closes the transport when its abort signal fires"
    - "contains a rejection that arrives after deadline closure"
    - "contains unexpected factory, DNS, and configuration-store errors"
    - "enforces one absolute deadline across database, DNS, transport, and send"
    - "pins a public DNS address while retaining the TLS hostname"
    - "rejects IPv4 and IPv6 non-public DNS answers"
    - "rejects acceptance resolved after the absolute deadline"
    - "requires explicit acceptance of the intended recipient"
### tests/server/platform-email-job-payload.test.ts  (5 cases)
  subject: "platform email job payload"  matches 5/5  (byte-exact for every case)
  categories: none (subject has 5 cases, 1 usable category)
    - [cipher] "preserves multibyte recipients"
    - [cipher] "rejects authenticated ciphertext tampering"
    - [cipher] "round trips one prepared delivery identity"
    - [cipher] "separates credential context and authenticates identity"
    - "purpose fixes the verification lifetime"
### tests/server/platform-email-resend-policy-cache.test.ts  (7 cases)
  subject: "resend policy reader"  matches 4/7  (fallback cases keep their name)
  categories: none (subject has 7 cases, 1 usable category)
    - [Valkey resend policy cache] "rejects delayed stale fills and refreshes equal revisions"
    - [Valkey resend policy cache] "replaces malformed data and validates replies"
    - [Valkey resend policy cache] "shares revisions across adapter instances"
    - "aborts stalled Valkey reads and refills"
    - "falls back to PostgreSQL when Valkey reads and refills fail"
    - "refills a cache miss with the PostgreSQL revision"
    - "uses a strict cache hit without reading PostgreSQL"
### tests/server/platform-email-settings-contract.test.ts  (3 cases)
  subject: "SMTP administration"  matches 2/3  (fallback cases keep their name)
  categories: none (subject has 3 cases, 0 usable category)
    - "contracts enforce resend cooldown bounds"
    - "returns contract-checked conflicts and strict request errors"
    - "mounted SMTP administration enforces super-admin auth and exact contracts"
