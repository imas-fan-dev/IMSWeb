# 执行计划：社区交换事务所移动端地图界面重设计

接口与取舍见 `design.md`。本文件只写顺序、命令与验收。

## 并发结构

三条轨道并行开跑，文件不重叠；T4/T5 在前三条收敛后串行。

| 波次 | 轨道 | 独占文件 |
| --- | --- | --- |
| W1 | T1 原生玻璃控件插件 | `apps/web/src-tauri/plugins/native-glass/**`、`apps/web/app/lib/native-glass-panel.ts`（新增）、`apps/web/tests/unit/lib/native-glass-panel.test.ts`（新增） |
| W1 | T2 地图控件与授权数据 | `apps/web/app/pages/community/exchange/exchange-office-map.tsx`、`exchange-office-map.css`、`community-exchange-map-section.tsx`、`apps/web/app/app.css`、`apps/web/tests/unit/pages/community/exchange/exchange-office-map.test.tsx` |
| W1 | T3 入口与 dialog | `apps/web/app/pages/community/exchange/community-exchange-page.tsx`、`components/exchange-mobile-navigation.tsx`、`components/exchange-map-attribution-dialog.tsx`（新增）、`apps/web/tests/unit/pages/community/exchange/components/exchange-mobile-navigation.test.tsx` |
| W1 收口 | 桌面授权入口 | `components/exchange-discovery-rail.tsx` + `community-exchange-page.tsx`（已交付：顶部工具区 `lg:hidden`、底栏 `md:hidden`，≥1024px 原本无入口） |
| W2 | T4 原生接线 | T2/T3 收敛后的同名文件 + `apps/web/app/components/app/app-tab-bar.tsx`（如需同步 tab bar 抑制） |
| W2 | T5 测试与文档 | `apps/web/tests/e2e/**`、`apps/web/tests/unit` 下除 T2/T3 已认领文件之外的测试、`docs/architecture/glass-refraction-platform-strategy.md` |

**同一文件同一时刻只允许一条轨道写入。** T4 开始前必须确认 W1 的三条轨道都已停止写文件。

## T1 原生玻璃控件插件

- [x] T1.1 枚举需要原生化的 Lucide 图标：从 T2/T3 的最终标记读取 `menu`、`x`、`map`、`list-filter`、`building-2`、`credit-card`、`user-round`、`locate-fixed`、`loader-circle`、`refresh-cw`、`info`（以实际 DOM 为准），在 `ios/Sources/Resources/Lucide.xcassets/` 补齐缺失的 `.imageset`，保持与现有 7 个图标相同的 `Contents.json` 结构。
- [x] T1.2 在 `src/models.rs`、`src/commands.rs`、`src/mobile.rs`、`src/desktop.rs` 增加 `set_controls` 命令，`desktop.rs` 沿用现有"不支持即返回 `supported: false`"的语义。
- [x] T1.3 在 `ios/Sources/` 新增原生控件渲染（可与 `NativeGlassPlugin.swift` 同文件或用新的 `GlassControlView.swift`）：`UIVisualEffectView` + `UIGlassEffect`；菜单用 `UIGlassContainerEffect` 把触发钮与展开面板放进同一个采样容器。
- [x] T1.4 实现 `hitTest` 透传容器（参照 `NativeTabBarOverlayView`），控件矩形外返回 `nil`。
- [x] T1.5 实现 `dispatchControl`，派发 `ims:native-glass-control`；菜单项带 `itemId`。
- [x] T1.6 补齐可访问性：`isAccessibilityElement`、`accessibilityTraits`、`accessibilityLabel`、禁用态。
- [x] T1.7 新增权限 `permissions/autogenerated/commands/set-controls.toml`，并登记进 `permissions/default.toml`。
- [x] T1.8 新增 `apps/web/app/lib/native-glass-panel.ts`，导出类型、事件常量、纯函数 `nativeGlassControlEvent`、`shouldUseNativeGlassControls`、`syncNativeGlassControls`。
- [x] T1.9 单测 `apps/web/tests/unit/lib/native-glass-panel.test.ts`：事件解析的合法与畸形输入、准入判断在 Web/Android/iOS 三组身份下的取值。

验证：`pnpm --filter @imsweb/web lint`；`pnpm run app:doctor`；`pnpm run app ios --target simulator`（构建阶段必须通过，观感验收留给 T4 后的设备步骤）。

## T2 地图控件与授权数据

- [x] T2.1 删除 `exchange-office-map.tsx` 的 `NavigationControl` import 与 `addControl`（约 `:11`、`:644-647`）。
- [x] T2.2 删除 `AttributionControl` import、`addControl` 与随后的程序化 `.click()`（约 `:625`、`:640-643`）。保留 `attributionControl: false`。
- [x] T2.3 不改动 `touchZoomRotate` / `dragPan` / `doubleClickZoom` / `keyboard` 的开关，也不改动 canvas 的 `touch-action` 来源。
- [x] T2.4 canvas 的 `aria-label`（约 `:512-517`）改为手势措辞，去掉"加减按钮"。
- [x] T2.5 定位按钮与错误气泡保留 DOM，给定位按钮加 `data-native-glass-control="locate"`，并确认外层容器不裁剪（`overflow` 不隐藏 tooltip 与原生覆盖层）。
- [x] T2.6 在 `exchange-office-map.tsx` 样式就绪后从 `map.getStyle().sources[*].attribution` 取值，交给 W0 已交付的 `parseMapAttribution`，经新增可选 prop `onAttributionChange` 上抛；按内容相等去重，卸载与加载失败时上抛 `null`。section 只做透传。
- [x] T2.7 清理 `exchange-office-map.css` 中失去消费者的 `.maplibregl-ctrl-group*` 规则、`.maplibregl-ctrl-bottom-right` 定位规则，以及 reduced-transparency 块中对应的选择器。
- [x] T2.8 清理 `app.css` 中 `[data-exchange-office-map] .maplibregl-ctrl-*` 区块。
- [x] T2.9 保留 `exchange-map-app-viewport::before`、三个 Sheet 的最大高度规则、以及 `exchange-map-app-control` / `exchange-map-app-surface`；数值与 `--glass-*` token 对齐，不新增第四套硬编码。

验证：`pnpm --filter @imsweb/web lint`；`pnpm run build`；浏览器手测捏合/双击/键盘缩放。

## T3 入口与 dialog

- [x] T3.1 只读消费 W0 已交付的 `exchange-map-attribution.ts`（类型 + `parseMapAttribution`），不修改其签名。
- [x] T3.2 新增 `components/exchange-map-attribution-dialog.tsx`，基于 `components/ui/dialog.tsx`，标题「地图数据来源」，按序渲染 text/link 段，链接 `target="_blank" rel="noopener noreferrer"`，正文中文。
- [x] T3.3 在 `community-exchange-page.tsx` 用 state 持有授权数据，渲染单个受控 dialog，并把数据与打开回调传给三处入口。
- [x] T3.4 App 折叠菜单新增「数据来源」条目（`localToolsOnly` 分支内），`InfoIcon` + `aria-haspopup="dialog"`，点击后收起面板并打开 dialog。
- [x] T3.5 菜单触发钮与面板容器加 `data-native-glass-control="map-tools"`；刷新按钮加 `data-native-glass-control="refresh"`。
- [x] T3.6 Web 桌面顶部标题卡（约 `:725-789`）新增授权入口图标按钮。
- [x] T3.7 Web 移动底部导航由 `grid-cols-5` 改为 `grid-cols-6`，新增授权条目；确认 375px 宽度下不溢出、每项触控目标 ≥ 44 × 44 CSS 像素。
- [x] T3.8 新入口的 `aria-label`、`aria-haspopup`、以及关闭后焦点回归触发控件。

验证：`pnpm --filter @imsweb/web lint`；`pnpm run test:web`；浏览器手测三处入口与 Escape/遮罩关闭。

## T4 原生接线（W2）

W1 已交付的实际接口（以代码为准，不要按早期命名写）：

- 命令名 `plugin:native-glass|set_controls`，权限文件 `permissions/autogenerated/commands/set_controls.toml`
- JS 客户端 `apps/web/app/lib/native-glass-panel.ts` 已导出类型、`NATIVE_GLASS_CONTROL_EVENT`、`nativeGlassControlEvent`、`syncNativeGlassControls`、`shouldUseNativeGlassControls`
- 坐标基准是 `webview.scrollView.adjustedContentInset`（T1 有意偏离 `safeAreaInsets`，避免 `viewport-fit=cover` 下重复叠加安全区）

- [x] T4.0 把菜单面板容器的 `data-native-glass-control="map-tools"` 换成 `data-native-glass-twin="map-tools"`，并让隐藏规则同时覆盖 `[data-native-glass-control]` 与 `[data-native-glass-twin]`；测量只取触发钮。

- [x] T4.1 新增消费 hook：读准入判断，首次 `setControls` 拿 `supported`；为真时在文档根写 `data-native-glass="controls"`，为假或插件报错时不写。
- [x] T4.2 在 `app.css` 增加 `html[data-native-glass="controls"] [data-native-glass-control] { display: none; }`，并确认 reduced-transparency 与 forced-colors 分支不被破坏。
- [x] T4.3 测量三个控件的 `getBoundingClientRect()`，连同圆角、图标、文案、状态组装成 `setControls` 载荷；圆角与尺寸从 CSS 读取或由 Web 侧常量传入，不在 Swift 侧硬编码。
- [x] T4.4 注册同步时机：挂载、`ResizeObserver`、`resize`、`orientationchange`、`visualViewport` 的 `resize`/`scroll`、菜单展开收起、状态变化、tab bar 显示/隐藏。**不注册地图 `move`/`moveend`。**
- [x] T4.5 监听 `ims:native-glass-control`，把 `press`/`menu-item` 映射到既有 handler（`locateUser`、菜单展开、刷新、`onShowMap`/`onOpenFilter`/`onOpenOffices`/`onOpenCards`/打开 dialog）。不新增业务路径。
- [x] T4.6 卸载时清空控件集合并移除文档根属性。
- [ ] T4.7 校准三个控件与 tab bar、安全区、顶部标题栏的间距，确保不重叠。
- [ ] T4.8 设备上重点核对 `adjustedContentInset` 这个坐标基准：有刘海与无刘海各看一次，控件是否与 DOM 版本同位；偏差超过 1pt 就回头改基准。

验证（设备，必须实际执行并记录结果）：

- [x] `pnpm run app:doctor`：全绿，仅 `TAURI_APPLE_DEVELOPMENT_TEAM` 未设置（只影响真机构建）。
- [x] `pnpm run app ios --target simulator --no-launch`：本机为 Xcode 27 + iOS 27 SDK，产物
      `gen/apple/build/arm64-sim/IMSWeb.app` 构建成功、安装到 iPhone 18 Pro 模拟器并启动正常，
      原生底部 tab bar 可见。这给了 Swift 控件代码此前缺失的编译与链接证据（`cargo check` 不编译 `ios/Sources/*.swift`）。
- [ ] 手动确认四处原生玻璃折射实时地图内容：本机无法在模拟器里打开地图页——打包版指向局域网 API 而本机未启动该 API，
      且 `simctl` 不能注入点击、应用也未注册 URL scheme。需要在有可用 API 的 iOS 26+ 设备/模拟器上补。
- [ ] 旋转、菜单展开、竖屏/横屏切换后控件位置正确
- [ ] 控件外区域的地图平移、缩放、点位点击正常

## T5 测试与文档（W2）

- [x] T5.1 删除 `community-exchange-map.spec.ts` 中 `.maplibregl-ctrl-compass` / `-zoom-in` / `-zoom-out` 断言，替换为"不存在任何 `.maplibregl-ctrl-group`"断言。
- [x] T5.2 重写 `app-map.spec.ts:336-372` 的 viewport 持久化用例：改用 canvas 上的 `wheel` 或 `dblclick` 触发缩放，等待 `moveend` 后断言 `mapBounds` 增长与 `viewport.zoom` 持久化。两种手势都试，保留在 Chromium mobile 上稳定的那种。
- [x] T5.3 新增授权 dialog 的 E2E：四个宽度区间各验证一次入口可达（<768px 底栏、768–1023px 顶部卡、≥1024px 发现侧栏），断言三个外链的 `href`（按 `NavigationLink` 规范化后的尾斜杠值）与可见文案，断言 Escape 与遮罩关闭。
- [x] T5.4 更新 `exchange-office-map.test.tsx` 的 `maplibre-gl` mock，移除已删除的 `AttributionControl` / `NavigationControl`。
- [x] T5.5 更新 `exchange-mobile-navigation.test.tsx`：新增「数据来源」条目的存在性与 `aria-haspopup`；确认 `grid-cols-6` 不会破坏既有断言。
- [x] T5.6 核对 `community-exchange-app-page.test.tsx` 与 `community-exchange-map-section.test.tsx`：两者都不引用被删除的
      MapLibre 控件，App 工具区仍断言刷新按钮带 `exchange-map-app-control size-10`，因此无需改动（完整 unit 套件通过）。
      该按钮的圆角在 T6.10 由 `rounded-lg` 改成 `rounded-full`。
- [x] T5.7 补充或调整 `exchange-map-attribution.test.ts`（W0 已交付 9 项用例，覆盖三链接顺序、实体解码、非 https 降级、wrapper 递归、script/style 丢弃、空输入返回 `null`）；本步只在实现暴露了新的边界情况时追加用例。
- [x] T5.8 新增单测文件不需要登记到 `apps/web/tests/unit/e2e/unit-source-policy.test.ts`：那里的 `memoryRouterBaseline` 是「直引 MemoryRouter 的旧文件」基线，只要求新测试不直引 `MemoryRouter`（用 `tests/unit/support/harness.tsx`），且基线不出现 stale 条目。确认新文件不违反该约束即可。
- [x] T5.9 更新 `docs/architecture/glass-refraction-platform-strategy.md`，记录原生例外扩展到页面内浮动控件、扩展边界、以及 §3 的 z 序约束。
- [x] T5.10 补两条授权 E2E（收口时发现 PRD 验收项没有对应断言）：375px 下底栏不横向溢出且六个条目
  都 ≥ 44×44 CSS 像素；授权文案缺失时 400 / 900 / 1280 三个宽度都不渲染入口，也不出现空 dialog。
- [x] T5.11 补 `apps/web/tests/unit/lib/native-glass-controls.test.tsx`（T4 的 provider 此前没有单测）：
  未准入时不调插件、无 provider 时不注册、仅当返回 `supported: true` 才写隐藏属性、`supported: false`
  与 reject 都回退、抑制期间推空集、菜单面板宽度与条目透传、原生事件只落到已注册控件、视口变化重同步、
  卸载时清空并移除属性。

已知的测试易踩点（两条都实际发生过）：

- 授权实测样式里的源**不能叫 `openmaptiles`**。边界合规会为同名源追加 `source-layer: "boundary"`
  的图层，MapLibre 对 GeoJSON 源上的 source-layer 直接报错，地图被换成不可用卡片，dialog 断言随之
  全数失败，而报错只出现在 `onFatalError`，不在控制台。实测源改用 `license-notice`。
- 交换工作区会顺带请求 `/api/wiki/catalog`（系列图标回退图）。它属于该页面的正常流量，需在 dispatcher
  里显式注册为确定性夹具，否则 teardown 会以未注册请求失败。

验证：`pnpm --filter @imsweb/web format`;`pnpm --filter @imsweb/web lint`;`pnpm run test:web`;`pnpm run test:web-routing`;`pnpm run build`;`pnpm run check:rules`。

## T6 药丸化（R6 追加，W3 串行）

目标：App 右下角的定位与地图工具合并为一个液态玻璃药丸，参照 Apple 地图的原生控件。

- [x] T6.1 协议新增可选 `group`：`native-glass/src/models.rs` 两个变体各加 `#[serde(default)] group: Option<String>`（Rust 是强类型的 serde 枚举，不加这一侧 Swift 收不到该字段），`GlassControlView.swift` 的 `NativeGlassControlPayload` 加 `let group: String?`，`native-glass-panel.ts`/`native-glass-controls.tsx` 两处 spec 透传。
- [x] T6.2 原生绘制：`GlassControlRenderer` 按 group 合并，胶囊取成员 frame 的并集、圆角 `min(宽, 高) / 2`；成员改为非玻璃的 `GlassControlButton`（不再各自绘制玻璃），相邻成员共享边画内缩 1pt 细线且 `isUserInteractionEnabled = false`；分组中的 menu 成员保留面板，面板作为胶囊的兄弟视图。
- [x] T6.3 DOM 形状：`exchange-office-map.css` 新增 `.exchange-map-app-pill-top` / `.exchange-map-app-pill-bottom`（外圆角 `9999px`、共享边直边且去边框、下段去 inset highlight 并用 `::before` 画内缩分隔线），两段都带上 `exchange-map-app-surface` 与 `exchange-map-app-control`。
- [x] T6.4 几何对齐：定位段与菜单触发段共用同一尺寸与同一 `md:right-2.5`（否则并集宽于单段、图标偏心）；错误提示气泡跟随定位段。最终尺寸见 T6.9。
- [x] T6.5 测试：`native-glass-controls.test.tsx` 新增分组透传与两段无缝相邻、未分组控件不带 group 两条；`exchange-map-styles.test.ts` 新增药丸样式契约（外圆角、共享边直边、分隔线内缩、规则位于 surface 之后）；`exchange-mobile-navigation.test.tsx` 更新类名断言。
- [x] T6.6 编译与设备验证：`cargo check --target aarch64-apple-ios-sim`（Rust 模型）与 `pnpm run app ios --target device --release`（唯一会编译 `ios/Sources/*.swift` 的路径），随后安装到 iPhone-texas 观察胶囊形状、分隔线、按压反馈与手势透传。
- [x] T6.7 面板圆角回归修复：药丸下段共享边是直角，而面板圆角一直复用触发段的 `cornerRadius`，展开后变直角。协议在三个（四个）面同步新增可选 `panelCornerRadius`：`models.rs` 的 menu 变体加 `#[serde(default)] panel_corner_radius: Option<f64>`、`GlassControlView.swift` 加 `let panelCornerRadius: Double?` 且缺失时回退到 `cornerRadius`、`native-glass-panel.ts` 的 menu 类型与 `native-glass-controls.tsx` 的采集各加一处；回归测试锁住触发段 0 / 面板 8 的分开上传。
- [x] T6.8 修复后重新构建安装：`TAURI_APPLE_DEVELOPMENT_TEAM` + `VITE_IMS_API_ORIGIN`/`VITE_IMS_PUBLIC_SITE_ORIGIN` 指向 preview，`pnpm run app ios --target device --device 00008150-001978A22208401C --release`，devicectl 报告安装并启动成功。
- [x] T6.9 药丸加大（追加）：两段由 `size-10` / `md:size-8` 改为 `size-12` / `md:size-10`（48px / 40px），定位段底偏移与错误气泡同步改为 `+3rem` / `md:+2.5rem`（等于段高，否则两段不再相邻），定位段图标由 Button 默认的 16px 统一为 `size-5`；`exchange-mobile-navigation.test.tsx` 的尺寸断言随之更新，原生胶囊与图标（`min(高 × 0.5, 22)`）随 Web 侧几何自动跟随。
- [x] T6.10 刷新按钮圆形化（追加）：App 工具区右上角只有这一颗刷新按钮，此前是 `rounded-lg` 的圆角方块，与同层的药丸观感不一致，改成 `rounded-full`。原生玻璃层从 DOM 孪生元素测量 `borderTopLeftRadius`（`native-glass-controls.tsx` 的 `measureCornerRadius`）再交给 `GlassControlView`，所以只改类名就让原生绘制同步变成圆形，Swift 与 Rust 两侧都不用动。测试：`community-exchange-app-page.test.tsx` 的类名断言更新为 `rounded-full`；`app-map.spec.ts` 新增「renders the app map refresh control as a circle」，在 App 目标下断言控件为正方形且计算圆角不小于半边长。反证：把类名改回 `rounded-lg` 时实测圆角 8px、要求 ≥ 20px，用例确实失败。
- [x] T6.11 刷新按钮原生玻璃归属复核（追加）：确认它已经完整走在原生玻璃链路上，Swift 与 Rust 不用改。`community-exchange-page.tsx` 的 `ExchangeRefreshButton` 用 `useNativeGlassControl("refresh", { kind: "icon-button", icon: "refresh-cw", label: "刷新交换区", disabled })` 注册，并给 DOM 孪生写 `data-native-glass-control="refresh"`；它渲染在 `NativeGlassControlsProvider`（739 行）与 `absolute z-20 lg:hidden` 的 `<section aria-label="地图工具">`（777 行）内部。`GlassControlView.swift` 对未分组控件走 `GlassControlButton(glassBacked: true)` → `UIGlassEffect(style: .regular)`，圆角直接用 DOM 孪生测量值；但「测量值」不等于「圆形」，T6.12 实测发现 `rounded-full` 的编译结果会把原生控件画成方块，`refresh-cw.imageset` 在 Lucide 目录中存在，`assetsAvailable` 不会把整组回退成 DOM。新增回归测试「hands the map toolbar refresh control to the native glass bridge」锁定 id / `refresh-cw` / label 与孪生标记（把 id 改名为 `refresh-probe` 时用例失败）。设备观感仍需真机确认：复核前真机上装的是 `be0b6dd6` 预览包，早于 `1062e073` 的原生化改造，因此看不到原生玻璃。

- [x] T6.12 原生圆角封顶（追加）：T6.11 里「`rounded-full` 经 DOM 测量就一定画成圆形」被实测推翻。iPhone 18 Pro 模拟器（iOS 27，debug 包 + `preview.idol-master.top`）上原生那组确实装上（未触发 DOM 孪生，底右胶囊带原生 1px 分隔线），但右上角刷新控件画成圆角方块。临时探针打出 `refresh=[340282001837565597733306976381245063168px]->3.402820018375656e+38`：Tailwind v4 把 `rounded-full` 编译成 `border-radius: 3.40282e38px`（FLT_MAX），`measureCornerRadius` 的 `parseFloat` 原样读出后经 `GlassControlView.swift:343` 落到 `layer.cornerRadius`。同屏 `map-tools=[0px]->0` 与一个未命名控件 `[8px]->8` 都正常，因为它们和定位按钮共享 `EXCHANGE_MAP_FLOATING_GROUP`，胶囊圆角由 Swift 按并集算（`GlassControlView.swift:386/395`），payload 圆角被忽略——这也解释了为什么只有刷新按钮出问题（它是唯一未分组的控件）。修复：新增 `readCornerRadius(raw, limit)` 把圆角卡在 `min(width, height)/2`（读不出数值仍返回 0），`measureCornerRadius` 用已测帧算 limit，`measurePanelRadius` 用面板自身 `offsetWidth/offsetHeight`。测试：`native-glass-controls.test.tsx` 新增「caps a fully rounded twin at half of its shorter side」（40×40 + FLT_MAX → 20），面板用例补 `offsetHeight` 桩。设备观感仍待真机复核。

验证：`pnpm --filter @imsweb/web run lint`;`pnpm --filter @imsweb/web exec vitest run tests/unit/lib/native-glass-controls.test.tsx tests/unit/pages/community/exchange/exchange-map-styles.test.ts tests/unit/pages/community/exchange/components/exchange-mobile-navigation.test.tsx`;`cargo check`;`pnpm run app ios --target device --release`。

## 收口检查

- [x] 全范围复检：`pnpm run check:root`、`pnpm --filter @imsweb/web run check`（lint + typecheck + unit + build）、
      `pnpm run test:web-routing`、`node --test tests/tauri-build-configuration.test.js tests/exchange-map-assets.test.js`、
      插件与 App 的 `cargo check --target aarch64-apple-ios-sim` 全部通过。API 侧改跑 pre-commit 同款的
      `check:architecture` 与 `tsc -p tsconfig.server.json`（本任务不触碰 API）。
      `pnpm run test:web` 的整套 Playwright 在 7 workers 下因并发负载失败 9 条 home 页面用例，
      以 `CI=1 --workers=1 --retries=0` 复跑同两个文件为 37 passed / 3 skipped / 0 failed。
- [x] 逐条核对 `prd.md` 的 Acceptance Criteria，未验证项显式标注原因（特别是设备观感项）。
- [x] 确认 `docs/architecture/glass-refraction-platform-strategy.md` 与实现一致，不留已失效的"仅导航层"表述。

## 回滚点

| 回滚点 | 范围 | 方式 |
| --- | --- | --- |
| R-A | 移除 `+/-` 与内嵌 attribution | 独立于原生改动，可单独保留 |
| R-B | 原生接线（T4） | 去掉 `data-native-glass="controls"` 的写入即回到纯 CSS 路径；T1 的插件代码可留在树上不影响 Web 与 Android |
| R-C | 整个任务 | 任务提交未合并前按提交边界回退 |

## 环境前置

- Node.js ≥ 22.13.0、pnpm 11，`pnpm install --frozen-lockfile`。
- 本地数据服务与迁移按 `docs/development/ai-environment.md`。
- iOS 原生验收需要 Xcode 26 与 iOS 26 SDK 的模拟器或真机；设备操作只走 `pnpm run app:*`，不直接调用 Tauri CLI。
