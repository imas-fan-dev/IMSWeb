# One-shot overlay for scripts/migration/legacy-inventory.sh. Export these
# alongside the authoritative API data-path environment. Production values must
# identify SQLite online backup files, never the live application databases.
IMS_INVENTORY_CORE_DB_PATH=
IMS_INVENTORY_STORY_DB_PATH=
IMS_INVENTORY_RUN_ID=
